# Crunch
Using a robot as a perceptron

## Setup

```bash
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
python test.py
```

**Notes:**
- PIR sensor needs ~30-60 seconds to stabilize after power on
- Sound sensor: adjust potentiometer until threshold LED just turns off
